{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "Sandbox settings template - strengthens security while minimizing impact on usability. Synchronized with docs/sandbox-allowlist-recipe.md (29-domain allowlist + 9-domain denylist)",
  "sandbox": {
    "enabled": true,
    "autoAllowBashIfSandboxed": true,
    "excludedCommands": [
      "docker",
      "docker-compose",
      "watchman",
      "systemctl",
      "launchctl",
      "brew services"
    ],
    "network": {
      "allowedDomains": [
        "github.com",
        "api.github.com",
        "raw.githubusercontent.com",
        "codeload.github.com",
        "objects.githubusercontent.com",
        "registry.npmjs.org",
        "api.anthropic.com",
        "pypi.org",
        "files.pythonhosted.org",
        "proxy.golang.org",
        "sum.golang.org",
        "crates.io",
        "static.crates.io",
        "rubygems.org",
        "api.firecrawl.dev",
        "firecrawl.dev",
        "techblog.zozo.com",
        "note.com",
        "assets.st-note.com",
        "zenn.dev",
        "qiita.com",
        "dev.to",
        "medium.com",
        "cdn-ak.f.st-hatena.com",
        "engineering.dena.com",
        "developers.cyberagent.co.jp",
        "tech.uzabase.com",
        "engineer.crowdworks.jp",
        "tech.smarthr.jp"
      ],
      "deniedDomains": [
        "169.254.169.254",
        "metadata.google.internal",
        "metadata.azure.com",
        "pastebin.com",
        "transfer.sh",
        "0x0.st",
        "paste.ee",
        "termbin.com",
        "ix.io"
      ]
    }
  },
  "permissions": {
    "allow": [
      "Bash(git *)",
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(pnpm *)",
      "Bash(yarn *)",
      "Bash(node *)",
      "Bash(python *)",
      "Bash(pip *)"
    ]
  },
  "_notes": {
    "purpose": "Recommended sandbox settings based on the Claude Code Permissions documentation",
    "autoAllowBashIfSandboxed": "Set to true to minimize permission prompts when sandbox is enabled",
    "excludedCommands": "Commands that must run outside the sandbox (require access to the host system)",
    "allowedDomains": "3-tier structure: dev core 14 (github / npm / anthropic / pypi / go / crates / rubygems) + Firecrawl core 2 + representative scrape targets 13 (Japanese/English tech blogs). Details: docs/sandbox-allowlist-recipe.md",
    "deniedDomains": "Blocks SSRF (cloud metadata endpoints) + exfiltration routes (pastebin-family). deniedDomains takes precedence over allowedDomains even if a domain appears in both",
    "sync_with": "Keep numbers and entries in exact sync with docs/sandbox-allowlist-recipe.md. Letting drift accumulate leaves users unable to tell which is authoritative — the template or the recipe"
  }
}

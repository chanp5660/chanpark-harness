# Verification 3.3 — patched harness binaries (BUILD_REF v4.16.4 + binary-rebuild.patch)

Date: 2026-07-07. Verified artifact: `out/harness-linux-amd64` (go1.25.5, CGO_ENABLED=0, -buildvcs=false, ldflags `-s -w -X main.version=4.16.4`).
Raw logs: `out/v33/v1-lowercase.log … v8-cjk.log`, sandbox projects under `out/v33/`.
Old binary reference: `/data/chanp5660/chanpark-harness/bin/harness-linux-amd64` (read-only).

## 3.2 artifacts

| File | Size | Platform (header + go buildinfo) | Version |
|------|------|----------------------------------|---------|
| harness-linux-amd64 | 11,608,248 B | ELF 64-bit x86-64 (`7f 45 4c 46`), GOOS=linux GOARCH=amd64 | `4.16.4 (Hokage)` |
| harness-darwin-amd64 | 11,826,240 B | Mach-O 64-bit x86_64 (`cf fa ed fe`), GOOS=darwin GOARCH=amd64 | stamped 4.16.4 |
| harness-darwin-arm64 | 11,208,018 B | Mach-O 64-bit arm64 (`cf fa ed fe`), GOOS=darwin GOARCH=arm64 | stamped 4.16.4 |
| harness-windows-amd64.exe | 11,924,992 B | PE32+ console x86-64 (`4d 5a`), GOOS=windows GOARCH=amd64 | stamped 4.16.4 |

All mode 0755. All `go version -m` show `CGO_ENABLED=0`, `-ldflags="-s -w -X main.version=4.16.4"`, go1.25.5, no vcs stamp (matches old binaries' `(devel)` shape). Note: new binaries are ~11.2-11.9 MB vs old 16.4-17.3 MB — old ones were evidently built without `-s -w`; stripping is upstream build-all.sh behavior and intentional.

## V1. Lowercase counting — PASS

Plans.md fixture: `cc:todo`, `cc:wip`, `cc:done`, `cc:blocked`, `cc:WIP` (upper), `cc:完了` (legacy alias), `pm:requested`, `pm:approved`.

- `hook session-init` (exit 0):
  `Plans.md: todo 1 / wip 2 / done 2 / blocked 1 / pm-requested 1`
  (wip 2 = cc:wip + cc:WIP; done 2 = cc:done + cc:完了 → case-insensitive + alias both correct)
- `hook plans-watcher` (exit 0) summary block:
  `pm:requested : 1 / cc:todo : 1 / cc:wip : 2 / cc:done : 2 / cc:blocked : 1 / pm:approved : 1`
  plus English "New task: request from PM" / "Task completed: ready to report to PM" and English `pm-notification.md` ("…Please review (cc:done)").
- `hook post-compact` (exit 0) re-injected exactly the active rows, lowercase honored:
  `| T1 lowercase todo | cc:todo |`, `| T2 lowercase wip | cc:wip |`, `| T4 lowercase blocked | cc:blocked |`, `| T5 uppercase legacy wip | cc:WIP |`

## V2. Legend English — PASS

On session-init output: `grep -c 依頼中` = **0**, `grep -c 確認済` = **0**, any-CJK = **0**.
Legend rows present: `| pm:requested | Requested by PM | Used in 2-agent workflow |` and `| pm:approved | Approved by PM | Reviewed and accepted |` (primary), compat note names no JP literal ("legacy Japanese and cursor:* marker forms are still counted as aliases").

## V3. Sync safety — PASS (one SSOT caveat for the Lead)

Ran patched `harness sync` on a full copy of the current repo (minus .git and bin/harness-*). Results:
- Files written by sync (complete list, via mtime diff): `.claude-plugin/plugin.json`, `.claude-plugin/settings.json`. Nothing else touched; **`.claude-plugin/hooks.json` NOT created**.
- plugin.json after sync: `name=chanpark-harness`, `version=1.2.2`, `displayName='ChanPark Harness'` preserved, `defaultEnabled=true` preserved, `skills=["./skills/"]`, `outputStyles="./output-styles/"`. Same key set as before (keys re-ordered alphabetically — cosmetic).
- settings.json regenerated byte-identical (no drift warning) — harness.toml [safety] matches the committed settings.json.
- **Caveat (expected SSOT behavior, needs repo edit before first real sync)**: sync OWNS description/keywords/author and regenerates them from `harness.toml [project]`, whose values still carry older text. Observed overwrites on the copy:
  - description: curated plugin.json text → `"Claude harness - A harness for solo developers (Vibecoders) …"` (from harness.toml)
  - keywords: `[…multi-agent, harness, orchestration]` → `[…vibecoder, workflow]` (from harness.toml)
  - author: `{name, email}` → `{name, url}` (harness.toml has url, not email)
  → Fix in repo before unbanning sync: update `harness.toml [project]` description/keywords and `[project.author]` (add email, or accept url) to match the curated plugin.json. Listed in repo-edits.md.

## V4. CI advice — PASS

`grep -ac 'ci-cd-fixer'` on binary = **0**; `grep -ac 'chanpark-harness:debugger'` = **3** (breezing signal, ci.go, ci_status_checker). Message shape: `[CI failure detected] … Recommended action: launch the chanpark-harness:debugger agent…`.

## V5. setup-codex / setup-opencode — PASS

`setup-codex.sh` occurrences = **0**; `setup-opencode.sh` occurrences = **0**. (The plain `backups/setup-codex` data-directory path name remains as a path constant, which references no script.)

## V6. TDD gate English — PASS (live-triggered)

Sandbox with `cc:wip` (lowercase!) in Plans.md + Write of `src/main.go`, no test edits → gate fired (also proves P1 on this path):
`{"decision":"approve","reason":"TDD reminder","systemMessage":"TDD is enabled by default. Write the test first when possible.\n\n…add a [skip:tdd] marker…\n\nThis is a warning only; it does not block the tool call."}` — fully English, exit 0.

## V7. Dispatch-table regression — PASS

- `--help` old vs new: **byte-identical** (out/v33/help.diff empty), both exit 0.
- `--version`: old `dev (Hokage)` → new `4.16.4 (Hokage)` (old was built without version stamping; new matches upstream VERSION).
- Representative `hook <name>` with `echo '{}'` stdin (coordinator's names session-start/setup-init don't exist in the dispatch table; nearest real subcommands used) — all **exit 0** with sane JSON/no-op:
  session-init → SessionStart JSON; memory-bridge → benign "unknown memory bridge target" notice; log-toolname → `{"continue":true}`; pre-tool → safe-fallback approve (same string as old binary); stop-evaluator → `{"ok":true,"systemMessage":"[StopSession] 2 WIP task(s) remain. Check Plans.md before stopping."}` (English + lowercase counting again); auto-cleanup → silent no-op; tdd-check → approve; post-compact → approve+re-inject; plans-watcher → empty PostToolUse.

## V8. CJK surfaces — PASS (P1–P5 surfaces clean; residual inventory documented)

- All exercised P1–P5 outputs (session-init, plans-watcher, pm-notification.md, post-compact, session-monitor, TDD gate, sync, stop-evaluator, CI advice strings): **0 CJK**.
- Binary-wide raw-byte CJK chars: 9,236 (old binary 13,628; −32%). Composition: Go runtime unicode tables (present in every Go binary, never emitted) + JP literals in ~35 non-target source files + intentional compat marker literals (`pm:依頼中` etc., used only as counting aliases).
- Remaining user-facing JP message emitters (targeted source grep, full list in out/v33/v8-cjk.log): fix_proposal_injector.go, task_completed_escalation.go (stderr escalation block), auto_cleanup_hook.go (maintenance warnings), todo_sync.go, inbox_check.go, file_lease_hook.go, session_auto_broadcast.go, posttooluse_commit_cleanup.go ([Commit Guard]), posttooluse_quality_pack.go, summary.go writeSummaryText (session end summary), setup_hook.go, lifecycle/tracker.go (error strings), task_completed.go (partial). None are P1–P5 targets; recommend a follow-up "full EN localization" task if desired.

## Verdict

| Check | Result |
|-------|--------|
| lowercase | PASS |
| legend | PASS |
| sync_safe | PASS (with harness.toml [project] alignment edit required before unbanning sync — see repo-edits.md) |
| ci_advice | PASS |
| codex_refs | PASS |
| tdd_english | PASS |
| dispatch_same | PASS |
| cjk_surfaces | PASS (P1–P5 scope) |

All 8 verifications PASS on the freshly built linux-amd64 artifact. Upstream `go test ./...` also passes on the patched tree (15/15 packages).
